{
  "name": "esg-agentic-rag-copilot-deploy",
  "private": true,
  "version": "1.0.0",
  "description": "Static frontend build helpers for Vercel and Cloudflare Pages",
  "scripts": {
    "build:static": "node scripts/build_static_frontend.mjs",
    "playwright:install": "playwright install chromium",
    "test:e2e": "playwright test",
    "test:e2e:headed": "playwright test --headed",
    "test:e2e:debug": "playwright test --debug"
  },
  "devDependencies": {
    "@playwright/test": "1.59.1"
  }
}
